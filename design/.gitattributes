# Source/working art library — large binaries tracked via Git LFS.
# Scoped to design/ so the small, web-ready exports under static/ stay as
# ordinary git objects (Hugo serves those directly).
#
# Almost nothing here is published: the exceptions are the handful of files
# the press kit offers, mounted one by one into static/press-kit/ by hugo.toml
# and pulled from LFS by .github/workflows/deploy.yml.
*.png   filter=lfs diff=lfs merge=lfs -text
*.svg   filter=lfs diff=lfs merge=lfs -text
*.kra   filter=lfs diff=lfs merge=lfs -text
*.blend filter=lfs diff=lfs merge=lfs -text
*.af    filter=lfs diff=lfs merge=lfs -text
